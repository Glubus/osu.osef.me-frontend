// types/global.ts

/**
 * URL de base pour toutes les requêtes API.
 * Peut être remplacée par une variable d'environnement si besoin.
 */
export const API_BASE_URL = "http://localhost:3001";

export const CATBOY_API_BASE_URL = "https://catboy.best";

/**
 * Mode maintenance - quand activé, affiche la page de maintenance pour toutes les routes
 */
export const MAINTENANCE_MODE = false;